import { TimeRangeSelector } from './index'

export default {
  title: 'TimeRangeSelector',
  component: TimeRangeSelector
}

const Template = (args: any) => <TimeRangeSelector {...args} />

export const FirstStory = {
  args: { }
}
