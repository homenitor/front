import { TemperatureGraph } from './TemperatureGraph'

export default {
  title: 'TemperatureGraph',
  component: TemperatureGraph
}

const Template = (args: any) => <TemperatureGraph {...args} />

export const FirstStory = {
  args: {}
}
